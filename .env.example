# CentOS 7 GlusterFS 3.4 Docker Compose Environment Variables
# このファイルをコピーして .env として使用してください

# Container Registry Settings
REGISTRY=ghcr.io
NAMESPACE=daicolo
IMAGE_NAME=centos7-gluster34

# Image Tag 
# 開発環境では 'latest' を使用
# 本番環境では具体的なバージョンを指定 (例: v0.1-20250731, v1.0.0)
IMAGE_TAG=latest

# Examples:
# IMAGE_TAG=latest
# IMAGE_TAG=v0.1-20250731
# IMAGE_TAG=v1.0.0
# IMAGE_TAG=develop

# Full image name (automatically constructed)
# Format: ${REGISTRY}/${NAMESPACE}/${IMAGE_NAME}:${IMAGE_TAG}
